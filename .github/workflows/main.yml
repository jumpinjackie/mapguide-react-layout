name: CI

on:
  push:
    branches: [ master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ master ]
  workflow_dispatch:

concurrency:
  group: gh-pages-deploy
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install

      - name: Audit packages (production packages only)
        run: yarn npm audit --environment production

      - name: Lint and run tests
        run: yarn run ci:gha

      - name: Coveralls
        uses: coverallsapp/github-action@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          path-to-lcov: ./coverage-report/lcov.info

      - name: Build size diff
        uses: q1sh101/build-size-diff@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          skip-install: true
          build-command: 'true'
          dist-path: 'viewer/dist'

      - name: Upload build artifacts
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: actions/upload-artifact@v4
        with:
          name: build-zips
          path: |
            viewer.zip
            storybook.zip

  deploy_gh_pages:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    permissions:
      contents: write

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-zips

      - name: Checkout gh-pages branch
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: gh-pages

      - name: Clear and extract viewer.zip to master/viewer
        run: |
          rm -rf gh-pages/master/viewer
          mkdir -p gh-pages/master
          unzip viewer.zip -d gh-pages/master

      - name: Clear and extract storybook.zip to master/storybook-latest
        run: |
          rm -rf gh-pages/master/storybook-latest gh-pages/master/storybook-extract
          mkdir -p gh-pages/master/storybook-extract
          unzip storybook.zip -d gh-pages/master/storybook-extract
          if [ -d gh-pages/master/storybook-extract/storybook-static ]; then
            mv gh-pages/master/storybook-extract/storybook-static gh-pages/master/storybook-latest
            rmdir gh-pages/master/storybook-extract
          else
            mv gh-pages/master/storybook-extract gh-pages/master/storybook-latest
          fi

      - name: Commit and push to gh-pages
        run: |
          cd gh-pages
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "Deploy master/viewer from ${{ github.sha }} (run ${{ github.run_id }})"
          git push

  release:
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'yarn'

      - name: Install dependencies and build
        run: yarn install

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body: Release ${{ github.ref_name }} (run ${{ github.run_number }})
          draft: true
          prerelease: true
          files: |
            viewer.zip
            storybook.zip
            apidocs.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
