/**
 * @deprecated Removed in 0.15. OL encoded expression evaluation is now handled
 * natively by OL's flat style rules. Use `ol/expr/cpu` for any custom expression
 * evaluation needs.
 *
 * @since 0.14
 * @since 0.15 File kept as a stub to avoid hard breaks in consumer code that may
 * import this path; the class is no longer used internally.
 */
// This module is intentionally empty.
export {};
