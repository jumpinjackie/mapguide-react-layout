import * as Constants from "../constants";
const assign = require("object-assign");

const INITIAL_STATE = { };

export function sessionReducer(state = INITIAL_STATE, action = { type: '', payload: null }) {
    return state;
}